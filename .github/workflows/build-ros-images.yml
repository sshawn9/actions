name: build-ros-images

on:
  workflow_dispatch:
    inputs:
      build_melodic:
        description: 'melodic'
        type: boolean
        default: true
      build_noetic:
        description: 'noetic'
        type: boolean
        default: true
      build_humble:
        description: 'humble'
        type: boolean
        default: true
      build_jazzy:
        description: 'jazzy'
        type: boolean
        default: true
      stage_base:
        description: 'base'
        type: boolean
        default: true
      stage_desktop:
        description: 'desktop'
        type: boolean
        default: true
      stage_box:
        description: 'box'
        type: boolean
        default: true

jobs:
  validate-and-gen-matrix:
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.gen.outputs.matrix }}
      count:  ${{ steps.gen.outputs.count }}
    steps:
      - id: gen
        shell: bash
        run: |
          set -euo pipefail

          distros=()
          if [[ "${{ inputs.build_melodic }}" == "true" ]]; then distros+=(melodic); fi
          if [[ "${{ inputs.build_noetic }}"  == "true" ]]; then distros+=(noetic);  fi
          if [[ "${{ inputs.build_humble }}"  == "true" ]]; then distros+=(humble);  fi
          if [[ "${{ inputs.build_jazzy }}"   == "true" ]]; then distros+=(jazzy);   fi
          if [[ ${#distros[@]} -eq 0 ]]; then echo "::error::At least one distro must be selected"; exit 1; fi

          stages=()
          if [[ "${{ inputs.stage_base }}"    == "true" ]]; then stages+=(base);    fi
          if [[ "${{ inputs.stage_desktop }}" == "true" ]]; then stages+=(desktop); fi
          if [[ "${{ inputs.stage_box }}"     == "true" ]]; then stages+=(box);     fi
          if [[ ${#stages[@]} -eq 0 ]]; then echo "::error::At least one stage must be selected"; exit 1; fi

          matrix='[]'
          for d in "${distros[@]}"; do
            for a in amd64 arm64; do
              matrix="$(jq -c --arg d "$d" --arg a "$a" '. + [{"distro":$d,"arch":$a}]' <<< "$matrix")"
            done
          done

          echo "count=$(jq 'length' <<< "$matrix")" >> "$GITHUB_OUTPUT"
          echo "matrix={\"include\":$matrix}"        >> "$GITHUB_OUTPUT"

  build:
    needs: validate-and-gen-matrix
    if: needs.validate-and-gen-matrix.outputs.count != '0'
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.validate-and-gen-matrix.outputs.matrix) }}
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-24.04' }}
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build
        shell: bash
        working-directory: build-ros-images
        env:
          DISTRO: ${{ matrix.distro }}
          GITEE_PAT: ${{ secrets.GITEE_PAT }}
        run: |
          set -euo pipefail
          source build.sh

          if [[ "${{ inputs.stage_base }}"    == "true" ]]; then build_base    "$DISTRO"; fi
          if [[ "${{ inputs.stage_desktop }}" == "true" ]]; then build_desktop "$DISTRO"; fi
          if [[ "${{ inputs.stage_box }}"     == "true" ]]; then build_box     "$DISTRO"; fi
